{
  "compilerOptions": {
    "module": "NodeNext", // Quy định output module được sử dụng
    "moduleResolution": "NodeNext",
    "target": "ESNext", // Target output cho code
    "allowJs": true, // Cho phép import file js
    "outDir": "dist", // Đường dẫn output cho thư mục build
    "esModuleInterop": true,// Quy định cách import module
    "strict": true /* Enable all strict type-checking options. */,
    "skipLibCheck": true /* Skip type checking all .d.ts files. */,
    //"experimentalDecorators": true /* Enables experimental support for ES7 decorators. */,
    "baseUrl": ".", // Đường dẫn base cho các import
    "paths": {
      "@/*": ["src/*"], // Đường dẫn tương đối cho các import (alias)
      "@modules/*": ["src/modules/*"],
      "@shared/*": ["src/shared/*"],
    }
  },
  "ts-node": {
    "require": ["tsconfig-paths/register"]
  },
  "include": ["src"],// Đường dẫn include cho các file cần build
  "exclude": ["node_modules"] // Đường dẫn exclude cho các file không cần build
}
