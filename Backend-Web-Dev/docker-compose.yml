
services:
  nodejs:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    container_name: nodejs_web_dev_container
    restart: unless-stopped
    ports:
      - 5002:5001
    expose:
      - 5002
#     volumes:
#       - nodejs_data:/app/backend/data

# volumes:
#   nodejs_data:

# run: docker compose down
# run: docker compose up -d
# run: docker compose up -d --build
# run: docker compose logs -f

# docker compose -p nodejs_web_dev_container up -d
# docker logs -f nodejs_web_dev_container (check log realtime)
# docker exec -it nodejs_web_dev_container sh
# exit (exit container)
# https://stackoverflow.com/questions/39632667/how-can-i-close-some-specific-port-on-linux
# Step 1:
# Open up cmd.exe (note: you may need to run it as an administrator, but this isn't always necessary), then run the below command:
# netstat -ano | findstr :<PORT>
# Step 2:
# Next, run the following command:
# taskkill /PID <PID> /F