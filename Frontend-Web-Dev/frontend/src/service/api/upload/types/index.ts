export type UploadImageResponse = {
  statusCode: number;
  message: string;
  data: {
    url: string;
  };
};

export type UploadVideoResponse = {
  statusCode: number;
  message: string;
  data: {
    url: string;
  };
};
